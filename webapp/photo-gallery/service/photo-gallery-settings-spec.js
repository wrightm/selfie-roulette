describe('photoGallerySettings', function () {

    beforeEach(module('photoGallery'));
    /*
     it('should ...', inject(function(photoGallerySettings) {

     //expect(photoGallerySettings.doSomething()).toEqual('something');

     }));
     */

});
