describe('countdownTimerService', function () {

    beforeEach(module('selfieRoulette'));

    it('should ...', inject(function (countdownTimerService) {

        //expect(countdownTimerService.doSomething()).toEqual('something');

    }));

});
