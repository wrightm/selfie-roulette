describe('photoGallery', function () {

    beforeEach(module('photoGallery'));

    it('should ...', inject(function (photoGallery) {

        //expect(photoGallery.doSomething()).toEqual('something');

    }));

});
